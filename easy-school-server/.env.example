# Server environment
# Copy to easy-school-server/.env before running the backend.

# HTTP server port
PORT=

# MongoDB connection string
MONGO_URI=

# JWT signing secret
JWT_SECRET=

# Comma-separated allowed frontend origins for CORS (e.g. http://localhost:3000,https://yourapp.com)
CORS_ORIGIN=

# SMTP configuration (optional when EMAIL_TEST_MODE=true)
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=

# true = email codes logged to server console; false = real SMTP delivery
EMAIL_TEST_MODE=

# Frontend API base URL used at build/runtime (optional in local dev)
VITE_API_URL=